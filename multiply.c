#include<stdio.h>
int main()
{
    float a,b,result;
    printf("enter the first floating value: ");
    scanf("%f",&a);
    printf("enter the second floating value: ");
    scanf("%f",&b);
    result=a*b;
    printf("the multiple of two floating value is: %f",result);
    return 0;
}